/** Shared Lucide icon for every Download / Export trigger.
 *
 * `ArrowDownToLine` reads cleaner than Lucide's tray `Download` at button size.
 * Import this instead of picking a lucide name per surface.
 */

import { ArrowDownToLine } from "lucide-react";

export const DownloadIcon = ArrowDownToLine;
